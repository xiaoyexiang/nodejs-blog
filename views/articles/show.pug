extends ../layout

block content
  h1= article.title
  h5 Written by #{author}
  p= article.body
  br
  hr
  if user && user.id == article.author
    a.btn.btn-primary.mr-2(href='/articles/'+ article._id +'/edit') Edit
    a.btn.btn-danger.delete-article(href="#", data-id=article._id) Delete